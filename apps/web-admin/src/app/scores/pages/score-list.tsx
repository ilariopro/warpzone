import { PageHeading } from '@warpzone/web-ui';

const ScoreList = () => {
  return (
    <PageHeading
      mainTitle="Scores"
      description="In this area of the site you can keep an eye on the scores and rankings"
    />
  );
};

export { ScoreList };
